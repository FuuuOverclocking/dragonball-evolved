- 开源反推闭源重构, 作为新一代闭源的骨架

核心目标:
- 异步的主循环, 异步的设备
- api 的反射, 生成
    - 配置文件表示: $schema, template
        - 额外信息: 字段布局
    - RESTful API 表示: OpenAPI yaml, API References (Web)
        - 端点 path
    - CLI/Repl
    - Req -> Resp, 类型递归分析, rustdoc
    - api 分类, 一类是命令 (副作用), 一类是声明 (纯) { Set, Get }
- 展示 logger 怎么写

次要目标:
- TUI 和 GUI
- 版本显示问题
    - main 上, 如果当前 commit 没有与 cargo 版本一致的 tag, 那么 minor + 1, -nightly
    - vXX.XX 分支上, 如果不一致, patch + 1, -nightly
